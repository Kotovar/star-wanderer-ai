/**
 * Глобальные параметры боя
 * Эти параметры используются как базовые значения и модифицируются
 * артефактами, умениями экипажа, трейтами и назначениями
 */

/**
 * Базовый шанс критического удара (в процентах)
 * Может быть увеличен артефактами (например, Critical Matrix)
 */
export const BASE_CRIT_CHANCE = 0.05; // 5% базовый шанс

/**
 * Базовый множитель критического урона
 * 2.0 = двойной урон при критическом ударе
 * Может быть увеличен артефактами (например, Overload Matrix)
 */
export const BASE_CRIT_MULTIPLIER = 2.0;

/**
 * Базовая точность оружия (в процентах)
 * Разная для разных типов оружия, модифицируется артефактами и умениями
 */
export const BASE_ACCURACY = {
    laser: 0.95,    // 95% базовая точность лазеров
    kinetic: 0.90,  // 90% базовая точность кинетического оружия
    missile: 0.80,  // 80% базовая точность ракет (могут быть перехвачены)
} as const;

/**
 * Минимальная точность (после всех модификаторов)
 */
export const MIN_ACCURACY = 0.50; // 50% минимальная точность

/**
 * Максимальная точность (после всех модификаторов)
 */
export const MAX_ACCURACY = 1.0; // 100% максимальная точность
