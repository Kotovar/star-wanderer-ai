// Grid-based placement configuration
export const SECTOR_GRID_CONFIG = {
    gridSize: 5, // 5x5 grid = 25 cells for better spacing (larger cells)
    cellPadding: 0.15, // Padding inside each cell (to avoid edges and overlap)
    centerExclusionRadius: 0.8, // Exclude cells within this radius from center (in cells)
};
